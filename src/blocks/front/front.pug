mixin front(modifier)
  .front&attributes(attributes)
    .container
      .row
        .col-lg-8.mx-auto
          .front__content
            h1.text-center.mb-15 Title
            p.front__dscr description